# This file requires all the necessary components
# to make use of the data object mappings.

require "json"
require "string_converter"
require "./api" # Needed for BASE_URL. Might be moved later.
require "./api/enum"
require "./mappings/*"
